name: Deploy to Surge

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install Surge
      run: |
        npm install --global surge

    - name: Deploy to Surge
      run: |
        surge --project ./ --domain <parcial-1>.surge.sh --token b41495963a75cbf290c2ae5b056551dc
